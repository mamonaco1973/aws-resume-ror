# ApplicationHelper is automatically included into every view in the app.
# Helper methods defined here become available as plain method calls in
# any .html.erb template — no import or include needed in the view.
#
# Common uses: formatting helpers, view-specific logic that is too small
# to warrant a presenter class, and shared partial helpers.
# This app currently has no shared view helpers; the module is left here
# as the conventional extension point.
module ApplicationHelper
end
